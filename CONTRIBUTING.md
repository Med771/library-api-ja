# Руководство по разработке Library API

## 🚀 Начало работы

### Предварительные требования

- Java 21+
- Maven 3.6+
- Git
- IDE (IntelliJ IDEA, Eclipse, VS Code)

### Настройка среды разработки

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd library-api
```

2. **Импорт в IDE**
- Откройте проект в вашей IDE
- Дождитесь загрузки зависимостей Maven

3. **Запуск в режиме разработки**
```bash
mvn spring-boot:run
```

## 📝 Стандарты кодирования

### Java Style Guide

- Используйте Java 21+ функции
- Следуйте Google Java Style Guide
- Максимальная длина строки: 120 символов
- Используйте 4 пробела для отступов

### Именование

- **Классы**: PascalCase (`BookService`)
- **Методы**: camelCase (`getPages`)
- **Константы**: UPPER_SNAKE_CASE (`MAX_PAGE_SIZE`)
- **Пакеты**: lowercase (`ru.ai.libraryapi`)

### Аннотации

- Используйте `@Slf4j` для логирования
- Добавляйте `@Valid` для валидации
- Документируйте публичные методы с `@Operation`

## 🧪 Тестирование

### Структура тестов

```
src/test/java/ru/ai/libraryapi/
├── BookCntTest.java          # Тесты контроллера
├── BookServTest.java         # Тесты сервиса
├── config/
│   └── BookCfgTest.java      # Тесты конфигурации
└── LibraryApiApplicationTests.java
```

### Запуск тестов

```bash
# Все тесты
mvn test

# Конкретный тест
mvn test -Dtest=BookCntTest

# С покрытием кода
mvn test jacoco:report
```

### Покрытие кода

Минимальное покрытие кода: **80%**

## 📚 Документация

### JavaDoc

Все публичные методы должны быть задокументированы:

```java
/**
 * Получение страниц из EPUB файла.
 * 
 * @param req запрос с путем к файлу и диапазоном страниц
 * @return результат с разобранными страницами
 * @throws IOException если произошла ошибка при чтении файла
 */
public ResDTO getPages(ReqDTO req) throws IOException {
    // implementation
}
```

### API Документация

- Используйте аннотации OpenAPI для описания эндпоинтов
- Добавляйте примеры запросов и ответов
- Описывайте коды ошибок

## 🔧 Конфигурация

### Профили

- `dev` - разработка (по умолчанию)
- `test` - тестирование
- `prod` - продакшн

### Переменные окружения

```bash
export SPRING_PROFILES_ACTIVE=dev
export APP_LIBRARY_DIR=/path/to/library
export APP_LIBRARY_MAX_LENGTH=1500
```

## 🐛 Отладка

### Логирование

Используйте структурированное логирование:

```java
log.info("Обработка запроса: path={}, from={}, to={}", 
         req.path(), req.from(), req.to());
```

### Уровни логирования

- `ERROR` - критические ошибки
- `WARN` - предупреждения
- `INFO` - общая информация
- `DEBUG` - отладочная информация

## 🚀 Развертывание

### Локальная сборка

```bash
mvn clean package
java -jar target/library-api-0.1.0.jar
```

### Docker

```bash
docker build -t library-api .
docker run -p 8080:8080 library-api
```

## 🤝 Процесс разработки

### Git Workflow

1. **Создание ветки**
```bash
git checkout -b feature/new-feature
```

2. **Коммиты**
```bash
git add .
git commit -m "feat: add new feature"
```

3. **Push и Pull Request**
```bash
git push origin feature/new-feature
```

### Типы коммитов

- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - документация
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - обслуживание

### Pull Request

При создании PR убедитесь, что:

- [ ] Код покрыт тестами
- [ ] Документация обновлена
- [ ] Нет ошибок линтера
- [ ] Все тесты проходят
- [ ] Добавлены примеры использования

## 📋 Чеклист для разработчика

### Перед коммитом

- [ ] Код отформатирован
- [ ] Добавлены тесты
- [ ] Обновлена документация
- [ ] Проверены линтеры
- [ ] Запущены все тесты

### Перед Pull Request

- [ ] Ветка актуальна с main
- [ ] Все тесты проходят
- [ ] Покрытие кода > 80%
- [ ] Документация обновлена
- [ ] Примеры добавлены

## 🆘 Получение помощи

- Создайте Issue в репозитории
- Обратитесь к команде разработки
- Изучите документацию Spring Boot
- Посмотрите примеры в Swagger UI

---

Удачной разработки! 🚀
