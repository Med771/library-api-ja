# Library API 📚

REST API для работы с EPUB книгами. Предоставляет функциональность для разбора EPUB файлов и получения их страниц в удобном для чтения формате.

## 🚀 Возможности

- **Разбор EPUB файлов** - Автоматическое извлечение и очистка контента из EPUB книг
- **Пагинация** - Получение страниц книги по диапазону
- **Очистка HTML** - Удаление ненужных элементов (изображения, стили, скрипты)
- **REST API** - Простой и понятный HTTP API
- **Swagger документация** - Автоматическая генерация документации API
- **Валидация данных** - Проверка входных параметров
- **Обработка ошибок** - Стандартизированные ответы об ошибках

## 🛠 Технологии

- **Java 21** - Основной язык программирования
- **Spring Boot 3.5.7** - Фреймворк для создания REST API
- **Epublib** - Библиотека для работы с EPUB файлами
- **JSoup** - Парсинг и очистка HTML контента
- **Lombok** - Сокращение шаблонного кода
- **Springdoc OpenAPI** - Автоматическая генерация Swagger документации
- **Maven** - Управление зависимостями и сборка проекта

## 📋 Требования

- Java 21 или выше
- Maven 3.6 или выше
- Операционная система: Windows, Linux, macOS

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd library-api
```

### 2. Сборка проекта

```bash
mvn clean compile
```

### 3. Запуск приложения

```bash
mvn spring-boot:run
```

Или используя Maven Wrapper:

```bash
./mvnw spring-boot:run
```

### 4. Создание исполняемого JAR

```bash
mvn clean package
java -jar target/library-api-0.1.0.jar
```

## 📁 Структура проекта

```
library-api/
├── src/
│   ├── main/
│   │   ├── java/ru/ai/libraryapi/
│   │   │   ├── LibraryApiApplication.java    # Главный класс приложения
│   │   │   ├── BookCnt.java                  # REST контроллер
│   │   │   ├── BookServ.java                 # Бизнес-логика
│   │   │   ├── ReqDTO.java                   # DTO для запросов
│   │   │   ├── ResDTO.java                   # DTO для ответов
│   │   │   └── config/                       # Конфигурационные классы
│   │   │       ├── BookCfg.java              # Конфигурация библиотеки
│   │   │       ├── WebCfg.java               # Веб-конфигурация
│   │   │       ├── SwaggerCfg.java           # Конфигурация Swagger
│   │   │       └── GlobalExceptionHandler.java # Обработка ошибок
│   │   └── resources/
│   │       └── application.yml               # Конфигурация приложения
│   └── test/                                 # Тесты
├── library/                                  # Директория с EPUB файлами
├── pom.xml                                   # Maven конфигурация
└── README.md                                 # Документация проекта
```

## 📖 API Документация

### Базовый URL

```
http://localhost:8080/api
```

### Swagger UI

Интерактивная документация API доступна по адресу:
```
http://localhost:8080/api/swagger-ui.html
```

### Эндпоинты

#### Получение страниц EPUB книги

**POST** `/api/epub/pages`

Получает страницы из EPUB файла по указанному пути и диапазону.

**Запрос:**
```json
{
  "path": "2222_2222/o23e-oew2-ewoe-1www/book.epub",
  "from": 0,
  "to": 5
}
```

**Параметры:**
- `path` (string, обязательный) - Путь к EPUB файлу относительно директории библиотеки
- `from` (integer, обязательный) - Начальная страница (индекс)
- `to` (integer, обязательный) - Конечная страница (индекс)

**Ответ:**
```json
{
  "pages": [
    [
      "<p>Содержимое первой страницы...</p>",
      "<h1>Заголовок</h1>",
      "<p>Еще один абзац</p>"
    ],
    [
      "<p>Содержимое второй страницы...</p>"
    ]
  ]
}
```

**Коды ответов:**
- `200 OK` - Успешное получение страниц
- `400 Bad Request` - Некорректные параметры запроса
- `500 Internal Server Error` - Внутренняя ошибка сервера

## ⚙️ Конфигурация

Основные настройки приложения находятся в файле `application.yml`:

```yaml
# Конфигурация приложения
app:
  library:
    # Директория с EPUB файлами относительно рабочей директории
    dir: library
    # Максимальный размер страницы в символах
    max-length: 1000

# Настройки сервера
server:
  port: 8080
  servlet:
    context-path: /api
```

## 🔒 Безопасность

Приложение использует CORS политику для ограничения доступа к API только с разрешенных доменов. По умолчанию разрешен доступ только с `https://example.com`.

Для изменения разрешенных доменов отредактируйте файл `WebCfg.java`:

```java
private static final String ALLOWED_ORIGIN = "https://your-domain.com";
```

## 📝 Примеры использования

### cURL

```bash
curl -X POST http://localhost:8080/api/epub/pages \
  -H "Content-Type: application/json" \
  -d '{
    "path": "2222_2222/o23e-oew2-ewoe-1www/book.epub",
    "from": 0,
    "to": 3
  }'
```

### JavaScript (Fetch API)

```javascript
const response = await fetch('http://localhost:8080/api/epub/pages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    path: '2222_2222/o23e-oew2-ewoe-1www/book.epub',
    from: 0,
    to: 3
  })
});

const data = await response.json();
console.log(data.pages);
```

### Python (requests)

```python
import requests

response = requests.post(
    'http://localhost:8080/api/epub/pages',
    json={
        'path': '2222_2222/o23e-oew2-ewoe-1www/book.epub',
        'from': 0,
        'to': 3
    }
)

data = response.json()
print(data['pages'])
```

## 🧪 Тестирование

Проект включает полный набор unit и интеграционных тестов с покрытием кода.

### Запуск тестов

```bash
# Все тесты
mvn test

# Только unit тесты
mvn surefire:test

# Интеграционные тесты
mvn verify

# С покрытием кода
mvn jacoco:report
```

### Использование скриптов

**Linux/macOS:**
```bash
chmod +x test.sh
./test.sh
```

**Windows:**
```cmd
test.bat
```

### Покрытие кода

Минимальное покрытие кода: **80%**

Отчет о покрытии доступен в `target/site/jacoco/index.html`

### Структура тестов

```
src/test/java/ru/ai/libraryapi/
├── BookCntTest.java                    # Unit тесты контроллера
├── BookCntIntegrationTest.java         # Интеграционные тесты контроллера
├── BookServTest.java                   # Unit тесты сервиса
├── ReqDTOTest.java                     # Тесты DTO запроса
├── ResDTOTest.java                     # Тесты DTO ответа
├── LibraryApiApplicationTests.java     # Тесты приложения
└── config/
    ├── BookCfgTest.java                # Тесты конфигурации
    ├── WebCfgTest.java                 # Тесты веб-конфигурации
    ├── SwaggerCfgTest.java             # Тесты Swagger конфигурации
    └── GlobalExceptionHandlerTest.java # Тесты обработки ошибок
```

## 📦 Сборка и развертывание

### Docker

**Сборка образа:**
```bash
docker build -t library-api .
```

**Запуск контейнера:**
```bash
docker run -p 8080:8080 library-api
```

**Использование docker-compose:**
```bash
docker-compose up -d
```

### Создание Docker образа

Dockerfile уже настроен для многоэтапной сборки:

```dockerfile
# Этап 1: Сборка
FROM maven:3.9-eclipse-temurin-21 AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn clean package

# Этап 2: Запуск
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### Развертывание в production

1. Соберите JAR файл: `mvn clean package`
2. Загрузите JAR на сервер
3. Запустите приложение: `java -jar library-api-0.1.0.jar`
4. Настройте reverse proxy (nginx, Apache) если необходимо

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции: `git checkout -b feature/new-feature`
3. Зафиксируйте изменения: `git commit -am 'Add new feature'`
4. Отправьте ветку: `git push origin feature/new-feature`
5. Создайте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🐛 Известные проблемы

- Приложение не поддерживает EPUB файлы с зашифрованным контентом
- Ограниченная поддержка сложных HTML структур в EPUB файлах
- Максимальный размер обрабатываемого файла ограничен доступной памятью

## 🔮 Планы развития

- [ ] Поддержка зашифрованных EPUB файлов
- [ ] Кэширование разобранных книг
- [ ] Поддержка различных форматов электронных книг (MOBI, AZW)
- [ ] Поиск по содержимому книг
- [ ] Метаданные книг (автор, название, ISBN)
- [ ] API для загрузки новых книг

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

- Создайте Issue в репозитории
- Отправьте email на support@example.com
- Обратитесь к документации Swagger UI

---

**Library API** - делаем чтение электронных книг проще! 📖✨
